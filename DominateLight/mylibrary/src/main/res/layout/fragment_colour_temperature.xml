<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data class="jeff.adjust.ColourTemperatureFragmentDB" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <jeff.widgets.CircleSeekBar
            android:id="@+id/colour_temperature_seekbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:padding="16dp"
            app:wheel_pointer_radius="8dp"
            app:wheel_reached_width="24dp"
            app:wheel_unreached_width="32dp" />

        <TextView
            android:id="@+id/colour_temperature_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textSize="18sp" />
    </RelativeLayout>
</layout>
